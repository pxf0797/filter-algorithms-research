# BS 仓位操作标识 — 设计文档

## 1. 需求摘要

在多周期 K 线主图上，按操作周期（可配置）标注 **B**（做多入场）/ **S**（做空入场）及对应的出场标记，并向下级周期级联。

## 2. 标记规则

| 场景 | 方向 | 颜色 | 标记文字 |
|------|------|------|----------|
| 做多入场 | long entry | 🟢 绿 | **B** |
| 做空入场 | short entry | 🔴 红 | **S** |
| 平多出场 | long exit | 🟢 绿 | **S** |
| 平空出场 | short exit | 🔴 红 | **B** |

> 颜色跟随**持仓方向**：多头相关标记全绿，空头相关标记全红。
>
> **标记来源优先级**：BS 标记优先从策略交易记录（`trade_records`）生成。策略未启用（`trade_records` 为空）时，回退到 Schmitt 原始信号对（`all_pairs`）。
>
> **交易序列**：
> - 做多：先 B(绿)入场，后 S(绿)出场
> - 做空：先 S(红)入场，后 B(红)出场

## 3. 新增侧边栏参数

**操作周期** — 下拉选择框，选项仅为当前视图已存在的周期（而非硬编码的全部 8 级周期）。只有 **操作周期** 及其**更低级别周期**的图表才显示 BS 标记。例如：若当前仅加载了日线、60分钟、15分钟三个视图，则下拉框只显示这三个选项。

## 4. 级联逻辑

定义反向层级映射（高→低）：
```
季线 → 月线 → 周线 → 日线 → 60分钟 → 15分钟 → 5分钟 → 1分钟
```

### 4.1 操作周期（本级）

BS 标记优先从策略交易记录（`trade_records`）生成；未启用策略时回退到 Schmitt 原始对（`all_pairs`）。

**来源一：策略交易记录（优先）**

当 `trade_records` 非空时，标记完全由交易记录生成：
- 做多序列：先 B(绿)入场（`entry_idx`），后 S(绿)出场（`exit_idx`，附 `exit_reason`）
- 做空序列：先 S(红)入场（`entry_idx`），后 B(红)出场（`exit_idx`，附 `exit_reason`）

**来源二：Schmitt 原始对（回退）**

当 `trade_records` 为空时：
- **入场：** Schmitt 信号对（`all_pairs`）的起点
  - sig=1 → 绿 **B**
  - sig=-1 → 红 **S**
- **出场：** pair 自然结束位置
  - long pair 结束 → 绿 **S**
  - short pair 结束 → 红 **B**

> 注：`exit_reason="take_profit"` 和 `"eod"` 也标记，但用不同符号（○）。

### 4.2 低一级周期（级联）

从操作周期逐级向下传递。每一级的 BS 标记依赖**紧邻高一级**的 BS 信号：

**入场级联：**
- 高一级给出入场 BS（如绿 B）→ 等本级 Schmitt 信号**同向**（也是 long）→ 在本级同向段起点标记 BS

**出场级联（多空对结束）：**
- 高一级 pair 结束 → 找本级对应 pair 的结束位置 → 标记 BS

**出场级联（偏离退出）：**
- 高一级 stop_loss 退出 → **立即**在本级对应时间位置标记 BS（不等本级确认）

## 5. 修改范围

### 5.1 新增文件
- `services/bs_marker.py` — BS 标记计算模块

### 5.2 修改文件
| 文件 | 改动 |
|------|------|
| `components/sidebar.py` | 新增 `TF_LOWER` 反向映射 + `操作周期` 下拉框 |
| `streamlit_app.py` | 渲染 BS 标记到 K 线子图；视图间传递 BS 级联数据 |
| `components/charts.py` | 新增 `_add_bs_markers()` 在 K 线子图上绘制 B/S 文字标注 |

### 5.3 不改动
- `services/filter_engine.py` — Schmitt 触发器、交易记录逻辑不变
- `config_db.py` / `db.py` — 数据层不变

## 6. 数据流

```
侧边栏选择 操作周期
       │
       ▼
┌──────────────────────────────────────┐
│ 遍历 4 个视图（TF 降序）              │
│                                      │
│  if TF > 操作周期 → 跳过（不标 BS）   │
│  if TF == 操作周期 → 从本级计算 BS    │
│  if TF < 操作周期 → 从高一级级联 BS   │
│                                      │
│  BS 数据存入 st.session_state        │
│  供下一级视图读取                     │
└──────────────────────────────────────┘
       │
       ▼
_render_chart() 接收 bs_markers 参数
       │
       ▼
_add_bs_markers() → Plotly annotations
       │
       ▼
K 线图上显示 B/S 标记
```

## 7. BS 标记在图表上的位置

- **横轴**：对应 K 线 bar 的索引位置
- **纵轴**：标注在 K 线实体上方（做多 B）或下方（做空 S），留适当偏移避免遮挡

## 8. 不处理的情况

- 操作周期以下没有更低周期时，仅在本级标注
- 同向判断不成立时（低一级没有同向信号），不在低一级标注入场 BS

## 9. 已知限制

### 9.1 Browse 模式时间窗口不匹配

不同周期（TF）的 K 线数据可能覆盖不同的时间范围。例如：日线可能覆盖 2024-01 至今，而 60 分钟线可能只覆盖最近 3 个月。当 BS 标记从高层级联到低层级时，`_find_date_index` 用于在低层级的日期数组中定位对应 bar：

- **目标日期早于数据范围**：返回 `0`（回退到第一个可用 bar）。这可能导致标记出现在 bar 0，而实际事件发生在数据范围之前。这是有意的折中 —— 相较于完全跳过标记，在第一个 bar 显示至少保留了信号的方向信息。
- **目标日期晚于数据范围**：返回 `None`（跳过标记），因为无法在当前视图内显示未来的标记。
- **异常**：任何日期解析/比较异常都静默回退为 `None`（跳过标记）。

该行为主要影响 browse 模式：当用户浏览历史时间窗口时，不同周期的数据覆盖范围会漂移，级联标记的时间对齐可能出现偏移。

### 9.2 级联仅支持单层

当前级联是从操作周期逐级向下传播（操作周期 → 紧邻低一级 → 更低级）。不支持跨级联（如日线直接级联到 5 分钟）。

## 10. 修复记录

### 日期对齐修复 (2024-07)

**问题**: 初始版本的级联使用纯索引对齐 —— 直接用高一级 bar 的索引号在低一级 bar 中查找。这在 browse 模式下不可靠，因为不同周期的 bar 数量和时间覆盖不同，索引号没有对应关系。

**修复**: 新增 `_find_date_index` 函数，改用日期对齐策略：

1. 将日期统一转换为 `pd.Timestamp` 进行可靠比较
2. 目标日期早于数据范围时返回 `0`（从可用数据起点开始级联）
3. 目标日期晚于数据范围时返回 `None`（跳过该标记）
4. 线性扫描查找第一个 `>=` 目标日期的 bar 索引
5. 异常捕获统一回退为 `None`

**影响**: `_compute_cascade_markers` 中的入场和出场级联逻辑现在依赖 `_find_date_index` 而非直接索引。标记在 browse 模式下能正确对齐到对应时间位置。

## 11. 测试覆盖

测试文件: `filter_app/tests/test_bs_marker.py`（42 个测试用例）

| 测试类 | 覆盖内容 | 用例数 |
|--------|----------|--------|
| `TestFindDateIndex` | 日期查找：早于/晚于/范围内/精确匹配/None/空 | 7 |
| `TestComputeOwnMarkers` | 操作周期直接标记：做多/做空/止损/多对/边界/交易记录优先/序列方向 | 11 |
| `TestCascadeMarkers` | 级联标记：入场同向/反向跳过/出场/止损立即/早日期/晚日期/None | 10 |
| `TestComputeBsMarkers` | 顶层调度：同周期/低周期级联/高周期跳过/无 higher_bs | 5 |
| `TestGetLowerTfs` | 周期链查询：日线/15分钟/最低/顶级/未知 | 5 |
| `TestTFHierarchy` | 层级映射：键值/链完整性/get_lower_tfs 一致性/无循环 | 4 |

运行方式:
```bash
cd filter_app && python3 -m pytest tests/test_bs_marker.py -v
```
