# PnL 与滤波价格关系 — 深度追溯报告

**问题**: "PnL 不就是基于滤波价格算出来的吗？滤波价格涨 PnL 就该涨，为什么不对？"

**结论**: **你的理解在公式层面是正确的，但你漏掉了一个关键前提。**

---

## 一、你的理解哪里对了？

**PnL 公式确实基于 filtered 价格。**

源码证据（`filter/engine/strategy.py`）：
```python
# 第86行：入场价 = 滤波价格
entry_price = filtered[entry_idx]

# 第150行：出场价 = 滤波价格
exit_price = filtered[exit_idx]

# 第156-158行：PnL 公式
trade_return = (exit_price - entry_price) / entry_price  # 做多
trade_return = (entry_price - exit_price) / entry_price  # 做空
```

在数学上，**只要你在持仓中，PnL 曲线精确跟踪 filtered 价格曲线**。这一点你没有理解错。

---

## 二、你的理解哪里漏了？

### 漏洞 1: filtered 不是真实可交易的价格

filtered 价格 = `filter(close)`，是对市场收盘价的**平滑/拟合**。

入场瞬间，filtered 和 close 之间**已经存在差距**。这个差距就是问题所在：

```
入场时:
  close    = 4.00 元  ← 你实际能在市场买到/卖出的价格
  filtered = 4.01 元  ← 回测系统假装你以这个价格成交的

出场时:
  close    = 4.02 元
  filtered = 4.03 元

回测显示 PnL = (4.03 - 4.01) / 4.01 = +0.50% ✅
真实可兑现 PnL = (4.02 - 4.00) / 4.00 = +0.50% (如果gap不变)
实际 gap 从 +0.01 扩大到 +0.02 → 真实 PnL 会被 gap 变化吃掉一部分
```

**76 笔交易的入场 gap 分布**：
| 分位数 | filtered - close 差距 |
|--------|----------------------|
| P10    | **-0.50%**（filtered 比 close 低 0.5%） |
| P50    | +0.01% |
| P90    | **+0.54%**（filtered 比 close 高 0.54%） |
| 极端   | **-1.27% ~ +0.69%** |

**案例 D 最直观**：一笔做空交易中，close 价格完全没动（涨跌幅 0.00%），但因为 filtered 相对 close 的 gap 从 -0.07% 变成了 +0.07%，回测系统仍然显示微亏。**纸面上的 gap 波动被当成了真实盈亏。**

### 漏洞 2: 信号引擎引入时机偏差

你不是"filtered 涨了就做多"，而是：

```
close  ──[filter]──>  filtered  ──[gradient]──>  速度(v)
                                    ──[gradient]──>  加速度(a)

入场条件: a > threshold  （加速度超过阈值，不只是方向）
出场条件: 信号反转 或 止损触发
```

**入场需要加速度（二阶导数）突破阈值**。这意味着：
- filtered 价格可能已经涨了一段，但加速度不够 → 不入场
- 等加速度够了 → filtered 已经涨了不少，**入场价已经偏离了最佳时机**
- 入场后加速度回落 → 立即出场 → 持有期很短，来不及覆盖 gap 的波动

### 漏洞 3: 非持仓期的空白

```
时间轴:
  |---持仓1---|---空仓---|---持仓2---|---空仓---|
  
  filtered 价格: 持续波动 ↗↘↗↘↗
  PnL 曲线:     ↗     平     ↘      平
```

**空仓期间 PnL 是平的**（你没持仓，不赚不赔），但 filtered 价格在持续波动。等下一笔交易入场时，filtered 已经从上一笔出场价变了——这个变化没有被计入 PnL，但它影响了新交易的入场价基准。

---

## 三、一个类比帮你记住

> **filtered 价格像是"天气预报"，PnL 像是你根据天气预报做的决策结果。**
>
> 天气预报说"明天升温 5°C"（filtered 涨了），你决定穿短袖出门（做多）。但实际温度只升了 1°C（close 涨得少），你冻了一天。
>
> 你问："我不是按照天气预报做的决策吗？为什么还是冻了？"
>
> 答案：因为天气预报 ≠ 实际温度。你决策的依据和实际承受的后果之间存在 gap。**回测系统假装你的决策依据就是实际温度**（PnL 用 filtered 算），但真实世界里不是。

---

## 四、根本原因总结

| # | 原因 | 为什么 PnL 不跟随 filtered |
|---|------|--------------------------|
| 1 | **filtered ≠ close** | filtered 是 close 的平滑拟合，两者在任一时间点都有差距。PnL 用 filtered 算，"看起来"盈利，但这部分利润包含 gap 波动 |
| 2 | **信号滞后** | 入场需要加速度突破阈值，等到信号触发时 filtered 已经移了一段，入场价不是"最优"价 |
| 3 | **空仓期不可见** | 持仓之间 filtered 持续波动，但 PnL 不记录这些变化 |
| 4 | **止损非价格驱动** | 止损用预测抛物线而非 filtered 价格水平，可能在 filtered 方向正确时就强制平仓 |
| 5 | **杠杆放大** | 系统存在动态仓位管理（`show_pnl_feedback`），trade_return 绝对值常远超价格变化%，放大了 gap 的影响 |

---

## 五、对你的策略意味着什么

1. **这不是"bug"** — 你无法让 PnL 完美跟踪 filtered，因为 filtered 本身不可交易。选择是：接受 gap 波动，或改用 close 价格计算 PnL（更贴近真实交易）

2. **gap 大小决定了策略的"可执行性"** — 入场 gap 的 P50 是 0.01%，但极端情况 ±1.27%。如果平均交易收益只有 0.5%，那 gap 波动就能吃掉大部分利润

3. **减少交易频率 = 减少 gap 暴露** — 每笔新交易都带入一个新的入场 gap。交易越少，gap 波动的累积影响越小

4. **如果要用 filtered 做真实交易信号** — 入场价应该用 close（你能实际成交的价格），而非 filtered。这会改变 PnL 计算，但更接近真实
